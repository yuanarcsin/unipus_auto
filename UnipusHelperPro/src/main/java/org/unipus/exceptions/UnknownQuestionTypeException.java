package org.unipus.exceptions;

/* 这里可以看，因为什么也没有 o(*￣▽￣*)o */

public class UnknownQuestionTypeException extends UnipusHelperException {
    public UnknownQuestionTypeException(String message) {
        super(message);
    }
}
